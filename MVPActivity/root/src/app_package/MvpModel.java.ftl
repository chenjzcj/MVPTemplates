package ${packageName}.${mvpModelPackage};

import ${mvpSuperModelPackage}.${mvpSuperModel};


/**
 * Created by ${userName} on ${.now}
 */
public interface ${modelClass} extends ${mvpSuperModel}{

  	interface Callback
    {
    
    }
    
}
