package ${packageName}.${mvpContractPackage};

import ${mvpSuperViewPackage}.${mvpSuperView};
import ${mvpSuperPresenterPackage}.${mvpSuperPresenter};

/**
 * Created by ${userName} on ${.now}
 */
public interface ${contractClass}{

  	interface View extends ${mvpSuperView}
    {
    
    }
    
    interface Presenter extends ${mvpSuperPresenter}
    {
    
    }
}
