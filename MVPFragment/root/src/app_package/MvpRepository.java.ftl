package ${packageName}.${mvpModelPackage};


/**
 * Created by ${userName} on ${.now}
 */
public class ${repositoryClass} implements ${modelClass}{

  	private Callback mCallback;
  	
  	public ${repositoryClass}(Callback callback)
    {
        this.mCallback=callback;
    }
    
}
