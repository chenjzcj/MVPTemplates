package ${packageName}.${mvpPresenterPackage};

import ${packageName}.${mvpContractPackage}.${contractClass};
import ${mvpBasePresenterPackage}.${mvpBasePresenter};

/**
 * Created by ${userName} on ${.now}
 */
public class ${presenterClass} extends ${mvpBasePresenter}<${contractClass}.View> implements ${contractClass}.Presenter{

 
}